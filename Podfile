platform :ios, '8.0'
inhibit_all_warnings! # 这行可以不要，如果你希望去除cocoaPods集成项目中的所有警告，就保留这一行代码
use_frameworks!
target 'LCUtils' do
    pod 'AFNetworking'
    pod 'MJRefresh'
    pod 'JSONModel'
    pod 'SDWebImage'
    pod 'Masonry'

end
